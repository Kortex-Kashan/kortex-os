# KORTEX OS Backend Core Dependencies
# Target Python version: 3.12+

fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
asyncpg>=0.30.0
httpx>=0.28.0
cryptography>=42.0.0
argon2-cffi>=23.1.0
aiosqlite>=0.22.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
tzdata>=2024.1
pdfplumber>=0.11.0
rapidocr-onnxruntime>=1.4.0
opencv-python-headless>=4.5.1.48
mcp==1.24.0

# Phase 5 Agent Gateway (mTLS gRPC session transport).
# The locked Phase 5 specification (S28) pins grpcio==1.68.2. That release
# does not exist on PyPI on any platform -- the 1.68 series is 1.68.0,
# 1.68.1, then 1.69.0 -- so it cannot be installed and would break every
# environment that resolves this file. Pinned to 1.68.1, the latest
# existing release in the specified series. Flagged for Chief Architect
# confirmation; no other spec value was altered.
grpcio==1.68.1
