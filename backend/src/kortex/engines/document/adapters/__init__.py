"""In-package Document Adapter plugins for the KORTEX OS Document Engine.

Modules in this package are discovered dynamically by DocumentAdapterLoader
(see kortex.engines.document.loader). Every module here should define one or
more concrete BaseDocumentAdapter subclasses.
"""

from __future__ import annotations
