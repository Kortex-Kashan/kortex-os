# Features

One folder per business feature: `<feature>/components`, `<feature>/hooks`,
and `<feature>/api.ts` (typed capability-call wrappers), per ADR-0002 §7.4.

No business features exist yet — this milestone is frontend-foundation
only; business screens are explicitly out of scope (see ADR-0002 §20
Non-Goals).
